import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
	<Layout>
		<div>
			<h1>NOT FOUND</h1>
			<p>
				The page you're serching for doesn&#39;t exist; perhaps try
				again?
			</p>
		</div>
	</Layout>
);

export default NotFoundPage;
